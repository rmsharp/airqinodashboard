# CLAUDE.md

## SESSION PROTOCOL — FOLLOW BEFORE DOING ANYTHING

**Read and follow `SESSION_RUNNER.md` step by step.** It is your operating procedure for every session. It tells you what to read, when to stop, and how to close out.

**Three rules you will be tempted to violate:**
1. **Orient first** — Read SAFEGUARDS.md -> SESSION_NOTES.md -> run `methodology_dashboard.py` -> git status -> report findings -> WAIT FOR THE USER TO SPEAK
2. **1 and done** — One deliverable per session. When it's complete, close out. Do not start the next thing.
3. **Auto-close** — When done: evaluate previous handoff, self-assess, document learnings, write handoff notes, commit, report, STOP.

`SESSION_RUNNER.md` documents known failure modes and their countermeasures. The protocol compensates for documented tendencies to skip orientation, skip close-out, and continue past the deliverable.

---

## Project-Specific Methodology Adaptations

*Additions and overrides to the base methodology at `SESSION_RUNNER.md` and `SAFEGUARDS.md` (synced from canonical, not project-owned). The base files govern unless explicitly overridden here. **Do not edit the synced files** — put customizations here.*

### Additional Phase 0 steps

(none)

### Additional task-to-workstream mappings

(none)

### Ledger (`CHANGELOG.md`) conventions

The rules are `docs/methodology/FRAMEWORK_APPARATUS.md` §The Action Ledger. This project adds two conventions:

- **Source tags.** `[BL-<id>]` names an item in *this* repo's `BACKLOG.md`. Work that comes from the methodology repo's backlog, such as a sync or BL-57's header migration, is tagged `[ad hoc]` and names the methodology item in its summary.
- **Legacy layout, left as written.** Session 6 migrated the header to `ledger-format: 2` (`5e4b483`) and dropped the old Keep-a-Changelog `## [Unreleased]` heading. So the 2026-09 entries sit under no month heading, and the first `## YYYY-MM` heading opens with the first 2026-10 entry. Don't add `## 2026-09` above them. The 2026-09-14 and 2026-09-15 entries have prose bodies instead of the detail bullets. They stay that way, because entries are never edited.

### Project-specific Learnings

<!-- This table is loaded every session and grows with no cap. When CLAUDE.md nears its
     size budget, extract these rows to a committed PROJECT_LEARNINGS.md at the project
     root and replace them with a one-line pointer (read on demand). See BOOTSTRAP.md Step 5. -->

| # | Learning | Source | When to Apply |
|---|----------|--------|---------------|
| 1 | Hardware assumptions from research papers may not match the actual device revision. Session 1 described "Arduino Mega + SIM900 GPRS" based on published literature; the actual REV6 board (2022) is a custom PCB with no USB port. Physical verification trumps literature-based assumptions for hardware projects. | Session 2 hardware investigation | When documentation references specific hardware components, verify against the physical device before writing connection instructions or buying parts. |
| 2 | Verify product availability before recommending a purchase. First adapter recommendation was out of stock, requiring a second round of research and a doc update. Check stock status as part of the recommendation, not after. | Session 2 adapter research | When recommending specific products for purchase. |
| 3 | A factual correction landed in one surface but not its duplicates. Session 2 fixed the "no USB / Arduino Mega" hardware claim in `serial_reader.py`, `.env.example`, and `docs/HARDWARE.md`, but the same stale "connect a USB cable to the Arduino Mega port" copy stayed in the UI (`templates/dashboard.html:54-55`) until Session 7. Doc fixes don't auto-propagate to user-facing strings. | Session 4 (found while writing README) | When correcting a factual/hardware claim, grep ALL surfaces for the old wording — templates, JS, docs, comments — not just the module that prompted the fix. |
| 4 | The harness's session-start git snapshot and `SESSION_NOTES.md`'s ACTIVE TASK both go stale here, because other actors commit to this repo between and during sessions. Session 4's Phase 0 snapshot missed an out-of-session edit. In Session 5, a methodology-repo session had switched the branch and committed `dfe26fd` after the snapshot was taken, and three June re-vendor commits had left the ACTIVE TASK wrong for three months. Methodology-repo sessions keep their notes in `~/Development/methodology`, not here (the session-notes boundary rule in `SESSION_RUNNER.md`). | Session 5 orientation | At every Phase 0, run `git status` and `git reflog -5` yourself instead of trusting the snapshot. For commits newer than the last airqino session, find their record with `git -C ~/Development/methodology log --all -S<sha>` before calling them ghost sessions. Search by hash: matching on commit subjects misattributed `66abe78` in Session 5. |
| 5 | The Flask UI can be runtime-verified without the device. With no data source configured (no `.env`, no uploaded CSV), `/` renders the setup banner. Start `python3 app.py` in the background, `curl` port 5001 and grep the HTML, then take a headless-Chrome screenshot (`--headless=new --screenshot=<png> --window-size=1200,700`). That checks a template change end to end. In Session 7, Chrome wrote the PNG within seconds but its process never exited, so run it in the background and stop it. | Session 7 banner fix | Any change to `templates/`, `static/` or a route's output. Check the rendered page, not only the source, then stop the server and Chrome and confirm port 5001 is free. |
| 6 | The session records cite commits by SHA. `CHANGELOG.md` entries, `HANDOFFS.md` receipts and the `git show <sha>:SESSION_NOTES.md` archive pointers all do. A squash or rebase merge rewrites those SHAs, and deleting the branch afterwards leaves every citation pointing at nothing. Session 8 merged PR #1 (`9099569`) and PR #2 (`8554078`) with merge commits for this reason. **Now a gate:** since Session 11, GitHub allows merge commits only on `rmsharp/airqinodashboard` (squash and rebase are off, per `gh repo view --json squashMergeAllowed,rebaseMergeAllowed`). The gate covers PR merges only, not local git. | Session 8 branch housekeeping | Merging any PR or branch into `main`: use a merge commit (`gh pr merge --merge`) or a fast-forward, never `--squash` or `--rebase`. Local squashes and rebases still work, so the rule still binds branches landed without a PR. Before deleting a branch, confirm with `git merge-base --is-ancestor` that it and every SHA the ledger cites are in `main`. |
| 7 | Reading code gives suspicions; running it gives facts. Session 9 read the three modules, then probed them in the scratchpad (the serial parser called directly, the routes through Flask's test client). The probes turned the suspicions into 7 reproduced defects (`docs/planning/test-suite-plan.md` §4). A spike then ran the plan's fixture, `pytest.ini`, pty and ratchet mechanics, driving 3 guards red, before the plan asserted any of them. Two defects were reasoned rather than probed until a post-write check, and 6 `file:line` citations written from memory were wrong. | Session 9 test-suite plan | Any plan or handoff that claims how this code behaves: probe each claim in the scratchpad (never in the repo) before writing it, and re-grep every `file:line` after writing, before the commit. |
| 8 | Close-out belongs at the end of the session, not the end of the deliverable. Session 9 closed out after committing its plan (`f49da64`). The operator then directed three more actions: a fast-forward of `main` with a push, a branch deletion, and a second push. I did each and recorded it in the ledger, but didn't run Phase 3 again. That left the `HANDOFFS.md` receipt calling the branch unpushed and telling the next session to land it. There was also no final report, and the operator had to ask for "a Phase 3 close-out at the end of each session". | Session 9 follow-on git work | Whenever work continues after a close-out: before the session's last reply, run Phase 3 again. Update the handoff, the receipt (still this session's, so overwrite it in place), the self-assessment and the ledger, then give the 3G report. |
| 9 | A guard test only guards where its hazard is present. The plan's Phase 1 red-drive, "turning `autouse` off fails T1.7", came from a Session 9 spike with a planted `.env`. On the operator's Mac, with no `.env`, the broken fixture still gave `9 passed`, because T1.7 checks that variables are absent and nothing set them. The gate form is `planted_leak` in `tests/test_dashboard_page.py`. It plants the hazard (all 8 variables, the 3 globals) in the test itself, so a broken `isolated` fails on every machine. | Session 10 red-drive probe | Any test that guards against something the environment usually lacks (a `.env`, a stray variable, leftover global state, a missing file): plant the hazard inside the test, then red-drive on a clean machine. A spike's result carries over only if the spike's setup comes with it. |
| 10 | An exit code read after a pipe belongs to the pipe's last command, not the one under test. The shell here is zsh, where bash's `${PIPESTATUS[0]}` expands to nothing. Session 11 reported `tail`'s exit code for a pytest run. Session 12 printed an empty `PIPESTATUS`, and later printed grep's `0` for a ratchet run that had exited `2`. Both were caught only by reading the output, and Session 11 had recorded the trap only as a self-assessment minus. | Sessions 11 and 12 | Any exit code a claim rests on (pytest, the ratchet, a red-drive): send the output to a scratchpad file with `> file 2>&1`, then `echo $?` straight after, with no pipe in between. |
| 11 | A real-fix red-drive tests the other tests as well as the xfail. In Session 13, a real D5 fix run against the whole suite flipped D5's strict xfail as intended. It also failed Phase 1's T1.5 and the first draft of Session 13's own D6 test. Both set only `AIRQINO_CLIENT_ID`, so both treated D5's behaviour as correct. That is the "pin today's buggy behaviour" the test plan rejects (§7), and the fix session would have had to rewrite them. | Session 13 red-drives | Any test that configures state next to a known defect: configure it fully (all four credentials, not one). When a strict xfail lands, red-drive a real fix against the whole suite (`tests/`), not one module. Any other test that fails is pinning the defect. |
| 12 | A green xfail isn't the user-visible fix. D7's xfail asserted only `status_code != 200`, and the test plan (§4) said the page reports non-2xx responses. In Session 15, a scratchpad probe of the server-only fix (503 plus the error) turned the test green, but a headless-Chrome screenshot showed the page stuck on "Loading readings…" indefinitely, worse than before: `loadCurrent` only logs a failed response. The probe came before the design, so the operator chose "server + page" on the evidence. | Session 15 D7 probe | Any fix for a user-visible defect: before choosing the design, run the candidate fix in the real app and look at the page (learning #5's method), not only after the commit. Treat a plan's claim about what the user sees as a claim to probe (learning #7). |
| 13 | A handoff's list of dashboard flags is a claim, not a checklist. Session 14 added BL-1, which raised a new LOW flag (the backlog done-mark format), and its handoff still called the flags unchanged. Session 15's Phase 0 grepped `dashboard.html` for the three flags that handoff named, so it missed the fourth, and the operator had to raise it. | Session 15 Phase 0 | At Phase 0 and at close-out, list every flag from the dashboard's output, e.g. every `risk-flag` div in `dashboard.html`. Don't grep for the ones you expect, and compare the count with the last handoff's. |
| 14 | An interaction needs a driver, not a screenshot. Learning #5's headless `--screenshot` shows the page as it loads, but D3's surface was an upload: the status line changes only after a file is dropped. In Session 16 a scratchpad Node 22 script drove headless Chrome over the DevTools protocol with Node's built-in `WebSocket`. Launch Chrome with `--headless=new --remote-debugging-port=9333 --user-data-dir=<scratchpad dir>`, take the page's `webSocketDebuggerUrl` from `http://127.0.0.1:9333/json/list`, send `Page.navigate`, then `Runtime.evaluate` a drop: `const dt = new DataTransfer(); dt.items.add(new File([csv], 'x.csv')); document.getElementById('uploadArea').dispatchEvent(new DragEvent('drop', {dataTransfer: dt, bubbles: true, cancelable: true}))`. Read `#uploadStatus` and `chart.data.datasets` back the same way, and save `Page.captureScreenshot`. It found the page's real symptom (a JSON parse error, not a 500) and let the operator compare three designs on the page. | Session 16 D3 probe | Any change the user reaches through an interaction (an upload, a click, a toggle): drive the interaction in the real app before choosing a design and again after the fix. Then stop Chrome and the app, and confirm port 5001 is free (learning #5). |

### Project-specific Failure Modes

(none — the base failure modes in `SESSION_RUNNER.md` apply.)
